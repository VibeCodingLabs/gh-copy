# WORKFLOWS

## Agent Workflow
When using `gh-copy` to feed an AI agent:
1. Run `gh-copy pack . -o prompt.txt`.
2. Upload `prompt.txt` to Claude/GPT-4.
3. Ask the agent to analyze the architecture or implement a feature based on the codebase context.
