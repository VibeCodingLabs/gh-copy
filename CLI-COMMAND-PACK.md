# CLI COMMAND: pack

`gh-copy pack` is the primary command used to pack a local or remote repository into a single AI-friendly file. It supports glob filtering, style selection, and token minification.