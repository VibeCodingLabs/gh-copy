# API ENDPOINT: pack_remote_repo

`pack_remote_repo(url, style, template, remove_empty_lines, include_patterns, exclude_patterns, mask_secrets)`

Downloads a GitHub repository archive to a temporary directory and passes it to `pack_repo`.