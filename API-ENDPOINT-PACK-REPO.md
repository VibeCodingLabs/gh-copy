# API ENDPOINT: pack_repo

`pack_repo(root_dir, style, template, remove_empty_lines, include_patterns, exclude_patterns, mask_secrets)`

The core programmatic interface for packing a local directory. Returns a dictionary containing the tree structure, the full string content, and the token count.